eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:11111/eureka/
spring:
  application:
    name: 21itemprovider
  cloud:
    config:
      profile: dev
      lable: master
      discovery:
        enabled: true
        service-id: 20configserver